# Project TV - Rocky Edition Custom ISO Kickstart
# Single %post block — everything in one section
cdrom
lang en_US.UTF-8
keyboard us
timezone Asia/Tokyo --utc
network --bootproto=dhcp --onboot=on --activate
rootpw --plaintext dre66m4n
user --name=howard --plaintext --password=dre66m4n --groups=wheel
ignoredisk --only-use=nvme0n1
zerombr
clearpart --all --initlabel --drives=nvme0n1
autopart --type=lvm --drives=nvme0n1
selinux --permissive
firewall --disabled
reboot

%packages
@core
@development
kernel-devel
git
gcc
make
curl
wget
rsync
tar
openssh-server
NetworkManager
%end

%post --log=/root/ks-post.log
echo "howard ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/howard
chmod 440 /etc/sudoers.d/howard
systemctl enable sshd || true
systemctl enable NetworkManager || true

cat > /etc/systemd/system/project-tv-firstboot.service << 'SVCEOF'
[Unit]
Description=Project TV First Boot Setup (KDE + extras)
After=network-online.target
Wants=network-online.target
ConditionPathExists=!/root/.project-tv-firstboot-done

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/root/project-tv-firstboot.sh
TimeoutStartSec=3600

[Install]
WantedBy=multi-user.target
SVCEOF

cat > /root/project-tv-firstboot.sh << 'SCRIPTEOF'
#!/bin/bash
exec > /root/project-tv-firstboot.log 2>&1
echo "=== Project TV First Boot Setup ==="
echo "Date: $(date)"
for i in $(seq 1 30); do
    ping -c 1 -W 3 dl.rockylinux.org >/dev/null 2>&1 && break
    echo "Waiting for network... ($i/30)"
    sleep 5
done
dnf install -y epel-release || true
dnf install -y dnf-plugins-core || true
dnf config-manager --set-enabled crb || true

# Install ZFS (sda/sdb preserved for mirrored pool, created later via installer module 02)
echo "Installing ZFS..."
dnf install -y https://zfsonlinux.org/epel/zfs-release-3-0.el10.noarch.rpm || true
dnf install -y kernel-devel-$(uname -r) || true
dnf install -y zfs-dkms zfs || true
modprobe zfs || true

echo "Installing KDE Plasma and tools..."
dnf install -y --allowerasing @"KDE Plasma Workspaces" plasma-desktop plasma-workspace sddm kwin dkms vim-enhanced bash-completion flatpak || true
# Retry core packages individually if group install had issues
rpm -q plasma-desktop >/dev/null 2>&1 || dnf install -y --allowerasing plasma-desktop sddm kwin plasma-workspace || true
rpm -q dkms >/dev/null 2>&1 || dnf install -y dkms || true
rpm -q flatpak >/dev/null 2>&1 || dnf install -y flatpak || true
systemctl set-default graphical.target
systemctl enable sddm.service || true
systemctl disable gdm.service 2>/dev/null || true
flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo || true
# Clone Project TV repo (bypass ksshaskpass which fails non-interactively)
if [ ! -d /home/howard/project_tv_rocky_linux/install.sh ]; then
    rm -rf /home/howard/project_tv_rocky_linux 2>/dev/null
    su - howard -c "GIT_ASKPASS='' GIT_TERMINAL_PROMPT=0 git clone https://github.com/metalllinux/project-tv-rocky-edition.git /home/howard/project_tv_rocky_linux" || true
fi
# If clone failed (private repo), the ISO mount copy below will be the fallback
chown -R howard:howard /home/howard/project_tv_rocky_linux 2>/dev/null || true
touch /root/.project-tv-firstboot-done
sync
echo "=== First boot setup complete. Rebooting into KDE... ==="
sleep 3
systemctl reboot
SCRIPTEOF

chmod +x /root/project-tv-firstboot.sh
systemctl enable project-tv-firstboot.service

# Copy Project TV from ISO mount (if available and not already cloned)
if [ ! -x /home/howard/project_tv_rocky_linux/install.sh ]; then
    for mnt in /run/install/repo /run/install/source /mnt/install/source; do
        if [ -d "${mnt}/project_tv_rocky_linux" ]; then
            rm -rf /home/howard/project_tv_rocky_linux 2>/dev/null
            cp -a "${mnt}/project_tv_rocky_linux" /home/howard/project_tv_rocky_linux
            chown -R howard:howard /home/howard/project_tv_rocky_linux
            chmod +x /home/howard/project_tv_rocky_linux/install.sh
            echo "Project TV copied from ${mnt}"
            break
        fi
    done
fi

# Create KDE autostart entry to launch installer on first login
mkdir -p /home/howard/.config/autostart
cat > /home/howard/.config/autostart/project-tv-installer.desktop << 'DESKTOPEOF'
[Desktop Entry]
Type=Application
Name=Project TV Installer
Comment=Launch Project TV installer on first login
Exec=konsole -e bash -c 'if [ -x ~/project_tv_rocky_linux/install.sh ]; then echo "=== Project TV - Rocky Edition Installer ==="; echo ""; echo "Run: sudo ./install.sh"; echo ""; cd ~/project_tv_rocky_linux; sudo ./install.sh; exec bash; else echo "Installer not found. Clone it with:"; echo "  git clone https://github.com/metalllinux/project-tv-rocky-edition.git ~/project_tv_rocky_linux"; exec bash; fi'
Terminal=false
X-KDE-autostart-phase=2
DESKTOPEOF
chown -R howard:howard /home/howard/.config

# Disable sleep/suspend in KDE Power Management
mkdir -p /home/howard/.config
cat > /home/howard/.config/powermanagementprofilesrc << 'PWREOF'
[AC][SuspendSession]
idleAction=0
suspendThenHibernate=false
suspendType=1

[AC][DPMSControl]
idleTimeout=0
lockBeforeTurnOff=0

[AC][DimDisplay]
idleTimeout=0

[AC][HandleButtonEvents]
lidAction=0
powerButtonAction=0
powerDownAction=0

[Battery][SuspendSession]
idleAction=0
suspendThenHibernate=false

[Battery][DPMSControl]
idleTimeout=0

[Battery][DimDisplay]
idleTimeout=0

[LowBattery][SuspendSession]
idleAction=0
suspendThenHibernate=false

[LowBattery][DPMSControl]
idleTimeout=0

[LowBattery][DimDisplay]
idleTimeout=0
PWREOF
chown howard:howard /home/howard/.config/powermanagementprofilesrc

# Disable systemd sleep/suspend targets as a belt-and-braces measure
systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target

echo "Kickstart post-install complete" > /root/ks-complete
%end
